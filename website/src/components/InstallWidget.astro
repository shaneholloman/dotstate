---
// Interactive install command widget with tabs
---

<div class="install-widget">
    <div class="install-header">
        <span class="install-title">┌─ INSTALL ─────────────────────────────────────────────────────────┐</span>
    </div>
    <div class="install-tabs">
        <button class="install-tab active" data-tab="curl">[1] curl</button>
        <button class="install-tab" data-tab="brew">[2] brew</button>
        <button class="install-tab" data-tab="cargo">[3] cargo</button>
        <button class="install-tab" data-tab="more">[?] more</button>
    </div>
    <div class="install-content">
        <div class="install-tab-content active" id="install-curl">
            <div class="code-block">
                <div class="code-header">
                    <span class="code-label">$ bash</span>
                    <button class="copy-btn" data-copy='curl -fsSL https://dotstate.serkan.dev/install.sh | bash"' aria-label="Copy install command">[cp]</button>
                </div>
                <code>curl -fsSL https://dotstate.serkan.dev/install.sh | bash"</code>
            </div>
        </div>
        <div class="install-tab-content" id="install-brew">
            <div class="code-block">
                <div class="code-header">
                    <span class="code-label">$ brew</span>
                    <button class="copy-btn" data-copy="brew tap serkanyersen/dotstate && brew install dotstate" aria-label="Copy install command">[cp]</button>
                </div>
                <code>brew tap serkanyersen/dotstate && brew install dotstate</code>
            </div>
        </div>
        <div class="install-tab-content" id="install-cargo">
            <div class="code-block">
                <div class="code-header">
                    <span class="code-label">$ cargo</span>
                    <button class="copy-btn" data-copy="cargo install dotstate" aria-label="Copy install command">[cp]</button>
                </div>
                <code>cargo install dotstate</code>
            </div>
        </div>
        <div class="install-tab-content" id="install-more">
            <div class="install-more-content">
                <p>[i] For more installation options, see the <a href="#installation" class="install-link">[+] Installation</a> page.</p>
            </div>
        </div>
    </div>
</div>

<style>
    .install-widget {
        background-color: var(--bg-secondary);
        border: 1px solid var(--accent-cyan);
        margin: 30px 0;
        overflow: hidden;
        box-shadow: 0 0 20px rgba(0, 212, 255, 0.2);
    }

    .install-header {
        background: linear-gradient(180deg, var(--bg-tertiary) 0%, var(--bg-secondary) 100%);
        padding: 8px 12px;
        border-bottom: 1px solid var(--border-dim);
    }

    .install-title {
        font-size: 0.75rem;
        color: var(--border-dim);
        font-family: var(--font-mono);
        overflow: hidden;
        white-space: nowrap;
        display: block;
    }

    .install-tabs {
        display: flex;
        border-bottom: 1px solid var(--border-dim);
        background-color: var(--bg-tertiary);
    }

    .install-tab {
        flex: 1;
        padding: 12px 15px;
        background: transparent;
        border: none;
        border-right: 1px solid var(--border-dim);
        color: var(--text-secondary);
        font-family: var(--font-mono);
        font-size: 0.85rem;
        cursor: pointer;
        transition: all 0.15s ease;
        text-align: center;
    }

    .install-tab:last-child {
        border-right: none;
    }

    .install-tab:hover {
        background-color: rgba(0, 212, 255, 0.08);
        color: var(--text-primary);
    }

    .install-tab.active {
        background-color: rgba(0, 255, 136, 0.1);
        color: var(--accent-green);
        font-weight: 600;
        border-bottom: 2px solid var(--accent-green);
    }

    .install-content {
        padding: 15px;
    }

    .install-tab-content {
        display: none;
        animation: fadeIn 0.2s ease-out;
    }

    .install-tab-content.active {
        display: block;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateX(-10px); }
        to { opacity: 1; transform: translateX(0); }
    }

    /* Code block styling */
    .code-block {
        background-color: var(--bg-primary);
        border: 1px solid var(--accent-cyan);
        width: 100%;
        box-sizing: border-box;
        box-shadow: 0 0 15px rgba(0, 212, 255, 0.15), inset 0 0 30px rgba(0, 0, 0, 0.3);
    }

    .code-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 15px;
        background: linear-gradient(180deg, rgba(0, 212, 255, 0.15) 0%, rgba(0, 212, 255, 0.05) 100%);
        border-bottom: 1px solid var(--accent-cyan);
    }

    .code-label {
        font-size: 0.85rem;
        color: var(--accent-cyan);
        font-family: var(--font-mono);
        font-weight: 600;
    }

    .code-block code {
        display: block;
        font-size: 1rem;
        color: var(--accent-green);
        background: none;
        padding: 18px 15px;
        white-space: pre-wrap;
        word-break: break-all;
        border: none;
        font-family: var(--font-mono);
        text-shadow: 0 0 8px rgba(0, 255, 136, 0.3);
    }

    .copy-btn {
        background: rgba(0, 212, 255, 0.1);
        border: 1px solid var(--accent-cyan);
        color: var(--accent-cyan);
        padding: 6px 12px;
        cursor: pointer;
        font-size: 0.8rem;
        font-family: var(--font-mono);
        transition: all 0.15s ease;
        line-height: 1;
    }

    .copy-btn:hover {
        background: rgba(0, 212, 255, 0.25);
        box-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
    }

    .install-more-content {
        text-align: center;
        padding: 20px 10px;
    }

    .install-more-content p {
        color: var(--text-secondary);
        margin: 0;
        font-size: 0.9rem;
    }

    .install-link {
        color: var(--accent-cyan);
        text-decoration: none;
        font-weight: 600;
        transition: color 0.15s ease;
    }

    .install-link:hover {
        color: var(--accent-green);
    }

    @media (max-width: 600px) {
        .install-tabs {
            flex-wrap: wrap;
        }

        .install-tab {
            flex: 1 1 auto;
            min-width: 70px;
            font-size: 0.8rem;
            padding: 12px 10px;
            min-height: 44px;
        }

        .install-header {
            display: none;
        }

        .install-content {
            padding: 12px;
        }

        .code-block code {
            font-size: 0.85rem;
            padding: 14px 12px;
            word-break: break-all;
        }

        .copy-btn {
            padding: 8px 14px;
            min-height: 36px;
        }
    }

    @media (max-width: 400px) {
        .install-tab {
            flex: 1 1 45%;
            min-width: 0;
            font-size: 0.75rem;
            padding: 10px 8px;
        }
    }
</style>
